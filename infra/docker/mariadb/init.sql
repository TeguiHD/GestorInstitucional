-- MariaDB init for dev — Prisma handles schema, this sets charset + timezone
SET NAMES utf8mb4;
SET time_zone = 'America/Santiago';

ALTER DATABASE asistencia CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
